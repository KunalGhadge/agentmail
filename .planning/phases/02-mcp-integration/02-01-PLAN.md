# Plan 02-01: MCP Integration

This plan covers the integration of the identity and storage layers into a functional MCP server.

## Tasks
- [x] Create `server.py` using `FastMCP`.
- [x] Implement `setup_identity` tool.
- [x] Implement `get_my_address` tool.
- [x] Implement `send_agent_mail` tool (with signature proof).
- [x] Implement `check_inbox` tool (pop-style).
- [x] External Verification: Verify the tools respond correctly via stdout mocks.

## Implementation Details
- Transport: STDIO (Standard input/output).
- Discovery: MCP Tool registration via decorators.
- Logic: Direct calls to `identity` and `storage` singleton instances.

## Verification
- Use `test_server_logic.py` to simulate tool calls.
